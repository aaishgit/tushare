<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="aaish" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/primary_dark_material_dark</item>
        <item name="android:colorBackground">@color/background_material_light</item>
        <item name="colorPrimaryDark">@color/statuscolor</item>
        <item name="android:colorForeground">@color/hint_foreground_material_light</item>
    </style>

    <style name="Theme.aaish.Light.NoActionBar" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimaryDark">#01012b</item>
    </style>

    <style name="aaish.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="aaish.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="aaish2" parent="aaish">
        <item name="android:colorBackground">#000000</item>
        <item name="android:textColorPrimary">#000000</item>
    </style>

    <style name="FilePickerTheme" parent="NNF_BaseTheme">
        <!-- Set these to match your theme -->
        <item name="colorPrimary">@color/bgColor</item>
        <item name="colorPrimaryDark">@color/statuscolor</item>
        <item name="colorAccent">@color/bgColor</item>

        <!-- Setting a divider is entirely optional -->
        <!--item name="nnf_list_item_divider">?android:attr/listDivider</item-->

        <!-- Need to set this also to style create folder dialog -->
        <item name="alertDialogTheme">@style/FilePickerAlertDialogTheme</item>

        <!-- If you want to set a specific toolbar theme, do it here -->
        <!-- <item name="nnf_toolbarTheme">@style/ThemeOverlay.AppCompat.Dark.ActionBar</item> -->
    </style>

    <style name="FilePickerAlertDialogTheme" parent="Theme.AppCompat.Dialog.Alert">
        <item name="colorPrimary">@color/bgColor</item>
        <item name="colorPrimaryDark">@color/statuscolor</item>
        <item name="colorAccent">@color/bgColor</item>
    </style>

</resources>
